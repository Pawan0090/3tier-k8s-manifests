global:
  namespace: apps

backend:
  replicaCount: 1
  image: "dockerpawan09/employee-backend"
  tag: "v2" # Jenkins will overwrite this dynamically
  port: 5000
  canaryWeight: 20 # percentage of traffic to route to new version initially

frontend:
  replicaCount: 1
  image: "dockerpawan09/employee-frontend"
  tag: "v2" # Jenkins will overwrite this dynamically
  port: 3000  

httpRoute:
  enabled: true
service:
  port: 3000
